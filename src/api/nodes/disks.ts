// Factory for /nodes/{node}/disks endpoints
import { Client } from "../../index.js";

export function disksFactory(client: Client) {
    return {
        // TODO: Implement disks endpoints
    };
}
