// Factory for /nodes/{node}/firewall endpoints
import { Client } from "../../index.js";

export function firewallFactory(client: Client) {
    return {
        // TODO: Implement firewall endpoints
    };
}
